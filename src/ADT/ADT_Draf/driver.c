#include <stdio.h>
#include "draf.h"

int main() {

    CreateDrafExtern(5);

    daftar();
    daftar();
    masuk();
    BuatDraf();
    BuatDraf();
    keluar();
    masuk();
    LihatDraf();
    LihatDraf();
    PrintKicau();    

    return 0;
}